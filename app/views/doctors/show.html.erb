<h1><%= @doctor.name %></h1>
<h3>Appointments</h3>

<ul>
<% @doctor.appointments.each do |appt| %>
  <li><%= link_to appt.patient.name, patient_path(appt.patient) %>: <%= appt.appointment_datetime.strftime('%B %d, %Y') %> at <%= appt.appointment_datetime.strftime('%H:%M') %></li>
<% end %>
</ul>
